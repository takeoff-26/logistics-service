spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: company
  profiles:
    active: local
  cloud:
    config:
      uri: http://config:8888
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://server:18999/eureka/
