spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: product
  profiles:
    active: local
eureka:
  client:
    service-url:
      defaultZone: http://localhost:18999/eureka/