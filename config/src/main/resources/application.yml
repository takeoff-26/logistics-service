spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/takeoff-26/logistics-service.git  # Git 저장소 URL
          default-label: feature/#113
          search-paths: config/configures/**

server:
  port: 19010

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18999/eureka/
