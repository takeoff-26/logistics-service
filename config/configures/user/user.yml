management:
  zipkin:
    tracing:
      endpoint: ${ZIPKIN_URL}
  tracing:
    sampling:
      probability: 1.0

server:
  port: 19021

springdoc:
  swagger-ui:
    enabled: true
    path: /docs
    url: /springdoc/openapi3-user-service.json
    operations-sorter: method
    tags-sorter: alpha
  api-docs:
    enabled: true
    path: /api-docs-user-service
