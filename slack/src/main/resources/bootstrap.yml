spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: slack
  profiles:
    active: local
  cloud:
    config:
      uri: http://config:8888
eureka:
  client:
    service-url:
      defaultZone: http://server:18999/eureka/
